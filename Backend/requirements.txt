# Backend dependencies for Enterprise AI Assistant
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
authlib>=1.2.0
itsdangerous>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
supabase>=2.0.0
pypdf>=3.0.0
# sentence-transformers>=2.2.0
numpy>=1.24.0
# Pillow>=10.0.0 (removed)
# OCR for images and scanned PDF pages (pulls torch ~2GB; omit only if you use image uploads without text)
# easyocr>=1.7.0 (removed to save memory)
# Render PDF pages to bitmaps for OCR when a page has little/no extractable text
pymupdf>=1.23.0
jinja2
python-multipart


groq>=0.4.0
bcrypt>=4.0.0
psycopg2-binary>=2.9.0
